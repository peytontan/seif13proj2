import React  from "react";

function Home() {
    return (
      <div>
        <p>Click on Information to search for client's Email</p>
        <p>Click on CSV to download csv for main/sub account creation</p>
      </div>
    );
}

export default Home;
